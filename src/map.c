
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdint-gcc.h>

#include <map.h>


map *createMap() {
	map *map1 = malloc(sizeof(map));
	initMap(map1);
	return map1;
}
void initMap(map *map) {

}
